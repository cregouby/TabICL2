% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rope.R
\name{apply_rotary_emb}
\alias{apply_rotary_emb}
\title{Apply rotary embeddings to a tensor}
\usage{
apply_rotary_emb(
  freqs,
  t,
  start_index = 1L,
  scale = 1,
  seq_dim = -2L,
  interleaved = TRUE
)
}
\arguments{
\item{freqs}{Frequency tensor for rotation. For interleaved mode, shape is
\verb{(..., dim)} where \code{dim = 2 * half}. For non-interleaved mode,
shape is \verb{(..., half)}.}

\item{t}{Input tensor to rotate}

\item{start_index}{Starting index for rotation in the last dimension
(default: \code{1L} for R's 1-based indexing)}

\item{scale}{Scaling factor for the rotation (default: \code{1.0})}

\item{seq_dim}{Sequence dimension (default: \code{-2L})}

\item{interleaved}{If \code{TRUE}, uses interleaved rotation where dimension
pairs are \verb{(1,2), (3,4)}, etc. If \code{FALSE}, uses non-interleaved rotation
where the embedding is split into first half \verb{[1:(d/2)]} and
second half \verb{[(d/2+1):d]} (default: \code{TRUE})}
}
\value{
Rotated tensor, same shape as \code{t}
}
\description{
Computes \eqn{x \cdot \cos(\theta) + \text{rotate}(x) \cdot \sin(\theta)}
for the portion of the input that overlaps with the frequency tensor.
}
\keyword{internal}
