% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layers.R
\name{skippable_linear}
\alias{skippable_linear}
\title{Linear layer that handles inputs flagged with a skip value}
\usage{
skippable_linear(in_features, out_features, bias = TRUE, skip_value = -100)
}
\arguments{
\item{in_features}{Integer. Size of each input sample.}

\item{out_features}{Integer. Size of each output sample.}

\item{bias}{Logical. If \code{FALSE}, the layer will not learn an additive bias
(default: \code{TRUE}).}

\item{skip_value}{Numeric. Value used to mark inputs that should be skipped
(default: \code{-100.0}).}
}
\value{
A \code{nn_module} that applies linear transformation with skip handling.
}
\description{
First applies the linear transformation to all inputs, then replaces outputs
for inputs where all values equal \code{skip_value} with the \code{skip_value}.
}
\examples{
\dontrun{
layer <- skippable_linear(in_features = 10L, out_features = 20L)
x <- torch_randn(32L, 10L)
x[1:5, ] <- -100.0  # Mark rows to skip
out <- layer(x)  # Skipped rows remain -100.0 in output
}

}
