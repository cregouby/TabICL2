% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssmax.R
\name{QASSMaxMLP}
\alias{QASSMaxMLP}
\title{QASSMaxMLP -- Query-Aware Scalable Softmax with MLPs}
\usage{
QASSMaxMLP(num_heads, head_dim, n_hidden = 64L, elementwise = FALSE)
}
\arguments{
\item{num_heads}{\code{integer(1)} Number of attention heads.}

\item{head_dim}{\code{integer(1)} Dimension of each attention head.}

\item{n_hidden}{\code{integer(1)} Number of hidden units in each MLP.
Default: \code{64L}.}

\item{elementwise}{\code{logical(1)} If \code{TRUE}, apply elementwise
scaling per head dimension.  Default: \code{FALSE}.}

\item{q}{\code{tensor} of shape \code{(bs, n_heads, seq_len, head_dim)}.
Query tensor after projection.}

\item{n}{\code{integer(1)} Source sequence length.}
}
\value{
An \code{\link[torch]{nn_module}} object (class generator).

Scaled query tensor, same shape as \code{q}.
}
\description{
Applies scaling to queries using two MLPs:

\eqn{q_{\text{scaled}} = q \cdot \text{base\_mlp}(\log n)
  \cdot (1 + \tanh(\text{query\_mlp}(q)))}

The \emph{base MLP} learns length-dependent scaling while the
\emph{query MLP} learns query-dependent modulation.  The query
MLP is zero-initialised so that the initial modulation is the
identity (\eqn{1 + 0 = 1}).

Apply QASSMax scaling to queries.
}
\seealso{
\code{\link{create_ssmax_layer}}
}
