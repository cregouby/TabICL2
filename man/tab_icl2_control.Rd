% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tab_icl2_control.R
\name{tab_icl2_control}
\alias{tab_icl2_control}
\alias{inference_config}
\title{Control TabICL2 inference execution}
\usage{
tab_icl2_control(
  device = NULL,
  use_amp = TRUE,
  use_fa3 = TRUE,
  verbose = FALSE,
  min_batch_size = 1L,
  safety_factor = 0.8,
  offload = "auto",
  ignore_pretraining_limits = FALSE,
  col_config = list(),
  row_config = list(),
  icl_config = list(),
  ...
)

inference_config(...)
}
\arguments{
\item{device}{A character string or \code{NULL} for the torch device (e.g.,
\code{"cpu"}, \code{"cuda"}, \code{"mps"}). Applied to all three transformer stages.}

\item{use_amp}{A logical. Enable automatic mixed precision. Applied to all
stages.}

\item{use_fa3}{A logical. Enable Flash Attention 3 kernels. Applied to all
stages.}

\item{verbose}{A logical. Enable verbose logging. Applied to all stages.}

\item{min_batch_size}{A positive integer. Minimum batch size passed to the
memory manager. Applied to all stages.}

\item{safety_factor}{A number in [0, 1]. Memory safety margin for batch
size estimation. Applied to all stages.}

\item{offload}{A logical or one of \code{"auto"}, \code{"gpu"}, \code{"cpu"}, \code{"disk"}.
Offload strategy for the column-embedding (COL) stage. Row-interaction
(ROW) and in-context-learning (ICL) stages default to \code{FALSE} unless
overridden via \code{row_config}/\code{icl_config}.}

\item{ignore_pretraining_limits}{A logical. Bypass the default data size
limits on training set rows (50,000) and predictors (2,000).}

\item{col_config}{A named list of additional settings for the COL stage,
overriding any of the defaults or parameters above.}

\item{row_config}{A named list of additional settings for the ROW stage.}

\item{icl_config}{A named list of additional settings for the ICL stage.}

\item{...}{Additional named arguments stored in the returned control object.
Use this for settings not covered above (e.g. \code{auto_offload_threshold},
\code{disk_offload_dir}, \code{use_async}, etc.) that apply uniformly. Per-stage
values must go in \code{col_config}, \code{row_config}, or \code{icl_config}.}
}
\value{
A list with class \code{"tab_icl2_control"} containing resolved
per-stage settings in \verb{$col}, \verb{$row}, and \verb{$icl}, plus
\verb{$ignore_pretraining_limits}.
}
\description{
Control TabICL2 inference execution
}
\examples{
tab_icl2_control()
tab_icl2_control(use_amp = FALSE, offload = "cpu")
tab_icl2_control(col_config = list(disk_offload_dir = tempdir()))
tab_icl2_control(ignore_pretraining_limits = TRUE)
}
