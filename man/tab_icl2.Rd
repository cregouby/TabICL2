% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tab_icl2_fit.R
\name{tab_icl2}
\alias{tab_icl2}
\alias{tab_icl2.default}
\alias{tab_icl2.data.frame}
\alias{tab_icl2.matrix}
\alias{tab_icl2.formula}
\alias{tab_icl2.recipe}
\title{Fit a TabICL2 model.}
\usage{
tab_icl2(x, ...)

\method{tab_icl2}{default}(x, ...)

\method{tab_icl2}{data.frame}(
  x,
  y,
  num_quantiles = 30L,
  version = NULL,
  control = control_tab_icl2(),
  ...
)

\method{tab_icl2}{matrix}(
  x,
  y,
  num_quantiles = 30L,
  version = NULL,
  control = control_tab_icl2(),
  ...
)

\method{tab_icl2}{formula}(
  formula,
  data,
  num_quantiles = 30L,
  version = NULL,
  control = control_tab_icl2(),
  ...
)

\method{tab_icl2}{recipe}(
  x,
  data,
  num_quantiles = 30L,
  version = NULL,
  control = control_tab_icl2(),
  ...
)
}
\arguments{
\item{x}{Depending on the context:
\itemize{
\item A \strong{data frame} of predictors.
\item A \strong{matrix} of predictors.
\item A \strong{recipe} specifying a set of preprocessing steps
created from \code{\link[recipes:recipe]{recipes::recipe()}}.
}}

\item{...}{Not currently used, but required for extensibility.}

\item{y}{When \code{x} is a \strong{data frame} or \strong{matrix}, \code{y} is the outcome
specified as:
\itemize{
\item A \strong{data frame} with 1 numeric column.
\item A \strong{matrix} with 1 numeric column.
\item A numeric \strong{vector} for regression or a \strong{factor} for classification.
}}

\item{num_quantiles}{An integer for the number of quantiles in case of regression.
Default is \code{30L}.}

\item{version}{A character string for the model version. Currently unsupported.}

\item{control}{A list of options produced by \code{\link[=control_tab_icl2]{control_tab_icl2()}}.}

\item{formula}{A formula specifying the outcome terms on the left-hand side,
and the predictor terms on the right-hand side.}

\item{data}{When a \strong{recipe} or \strong{formula} is used, \code{data} is specified as:
\itemize{
\item A \strong{rsplit} object from \code{rsample} package containing both the predictors and the outcome.
}}
}
\value{
A \code{tab_icl2} object with elements:
\itemize{
\item \code{fit}: the object containing the model.
\item \code{levels}: a character string of class levels (or NULL for regression)
\item \code{training}: a vector with the training set dimensions.
\item \code{blueprint}: am object produced by \code{\link[hardhat:mold]{hardhat::mold()}} used to process
new data during prediction.
}
}
\description{
\code{tab_icl2()} applies data to a pre-estimated deep learning model defined by
Qu \emph{et al} (2026). This model emulates Bayesian inference for
regression and classification models.
}
\details{
\subsection{Computing Requirements}{

This model can be used with or without a graphics processing unit (GPU).
However, it is fairly limited when used with a CPU (and no GPU). There might
be additional data size limitation warnings with CPU computations, and,
understandably, the execution time is much longer. CPU computations can also
consume a significant amount of system memory, depending on the size of your
data.

GPUs using CUDA (Compute Unified Device Architecture) are most effective.
Limited testing with others has shown that GPUs with Metal Performance
Shaders (MPS) instructions (e.g., Apple GPUs) have limited utility for these
specific computations and might be slower than the CPU for some data sets.
}

\subsection{Data}{

Predictors do not require preprocessing; missing values and factor vectors
are allowed.
}

\subsection{Model Selection}{
\subsection{Selecting a model version}{

Use the \code{version} argument to select a specific pretrained model version. For
example:

\preformatted{
  # Use classifier version 2
  mod <- tab_icl2(predictors, outcome, version = "tabicl_classifier_v2")

  # Use regressor version 2
  mod <- tab_icl2(predictors, outcome, version = "tabicl_regressor_v2")
}
}

\subsection{Pointing to a local model file}{

If you have a model file on disk (e.g., downloaded for offline use), pass
its path via \code{control_tab_icl2(model_path = ...)}:

\preformatted{
  ctrl <- control_tab_icl2(model_path = "/path/to/model_file.ckpt")
  mod  <- tab_icl2(predictors, outcome, control = ctrl)
}

Note that \code{version} and \code{model_path} are mutually exclusive: if \code{version}
is set, it overwrites any \code{model_path} supplied through \code{control}.
}

}

\subsection{Calculations}{

For the \code{softmax_temperature} value, the softmax terms are:

\preformatted{
exp(value / softmax_temperature)
}

A value of \code{softmax_temperature = 1} results in a plain softmax value.
}
}
\examples{
predictors <- mtcars[, -1]
outcome <- mtcars[, 1]
mtcars_split <- rsample::initial_split(mtcars)

\dontrun{
if (torch_is_installed() & interactive()) {
 # XY interface
 mod <- tab_icl2(predictors, outcome)
 mod

 # Formula interface
 mod2 <- tab_icl2(mpg ~ ., mtcars_split)
 mod2

 # Recipes interface
 if (rlang::is_installed("recipes")) {
  suppressPackageStartupMessages(library(recipes))
  rec <-
   recipe(mpg ~ ., mtcars) \%>\%
   step_log(disp)

  mod3 <- tab_icl2(rec, mtcars_split)
  mod3
 }
}
}

}
\references{
Jingang Qu, David Holzmüller, Gaël Varoquaux, Marine Le Morvan.
"TabICLv2: A better, faster, scalable, and open tabular foundation model."
\emph{arXiv preprint} arXiv:2602.11139 (2026).

Jingang Qu, David Holzmüller, Gaël Varoquaux, Marine Le Morvan.
"TabICL: A Tabular Foundation Model for In-Context Learning on Large Data." \emph{arXiv preprint}
arXiv:2502.05564 (2025).
}
\seealso{
\code{\link[=control_tab_icl2]{control_tab_icl2()}}, \code{\link[=predict.tab_icl_v2]{predict.tab_icl_v2()}}
}
