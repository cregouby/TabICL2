% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rope.R
\name{rotate_half_interleaved}
\alias{rotate_half_interleaved}
\title{Interleaved rotation: pairs (1,2), (3,4), (5,6), ... (1-based indexing)}
\usage{
rotate_half_interleaved(x)
}
\arguments{
\item{x}{Input tensor of shape \verb{[.., d]} where \code{d} is even}
}
\value{
Rotated tensor with same shape as \code{x}
}
\description{
Given input \verb{[.., d]}, rearranges to \verb{[.., d/2, 2]} and rotates pairs.
Used by default in most RoPE implementations (e.g. LLaMA).
}
\details{
In Python (0-based): pairs are (0,1), (2,3), ...
In R (1-based): pairs are (1,2), (3,4), ... — same logical pairing,
just different index notation.
}
\keyword{internal}
