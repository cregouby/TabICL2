% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/attention.R
\name{sdpa_with_flattened_batch}
\alias{sdpa_with_flattened_batch}
\title{Apply scaled dot-product attention with flattened batch dimensions}
\usage{
sdpa_with_flattened_batch(
  q,
  k,
  v,
  attn_mask = NULL,
  dropout_p = 0,
  ssmax_layer = NULL
)
}
\arguments{
\item{q}{Query tensor of shape \verb{(.., nh, tgt_len, hs)} where:
\itemize{
\item \code{..} represents arbitrary batch dimensions
\item \code{nh} is the number of attention heads
\item \code{tgt_len} is the target sequence length
\item \code{hs} is the head size (embedding dimension per head)
}}

\item{k}{Key tensor of shape \verb{(.., nh, src_len, hs)} with matching batch dimensions.}

\item{v}{Value tensor of shape \verb{(.., nh, src_len, hs)} with matching batch dimensions.}

\item{attn_mask}{Optional attention mask of shape \verb{(.., nh, tgt_len, src_len)}.}

\item{dropout_p}{Numeric. Dropout probability applied to attention weights
(default: \code{0.0}).}

\item{ssmax_layer}{Optional \code{nn_module}. If provided, applies scalable softmax
(SSMax) scaling to queries before attention computation.}
}
\value{
Attention output tensor of shape \verb{(.., nh, tgt_len, hs)} preserving
the original batch dimensions of the input.
}
\description{
This function handles arbitrary batch dimensions by flattening them before
applying PyTorch's \code{scaled_dot_product_attention} and then reshaping the
output back to the original shape. This flattening is necessary to properly
trigger Flash Attention.
}
\keyword{internal}
