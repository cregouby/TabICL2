% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/attention.R
\name{multi_head_attention_forward}
\alias{multi_head_attention_forward}
\title{Multi-head attention with support for rotary position embeddings}
\usage{
multi_head_attention_forward(
  query,
  num_heads,
  in_proj_weight,
  in_proj_bias,
  dropout_p,
  out_proj_weight,
  out_proj_bias,
  key = NULL,
  value = NULL,
  cached_kv = NULL,
  training = TRUE,
  key_padding_mask = NULL,
  attn_mask = NULL,
  rope = NULL,
  ssmax_layer = NULL,
  need_kv = FALSE
)
}
\arguments{
\item{query}{Query tensor of shape \verb{(.., tgt_len, embed_dim)}.}

\item{num_heads}{Integer. Number of attention heads.}

\item{in_proj_weight}{Tensor. Combined weight matrix for Q, K, V input projections.}

\item{in_proj_bias}{Tensor or NULL. Combined bias vector for input projections.}

\item{dropout_p}{Numeric. Dropout probability applied to attention weights.}

\item{out_proj_weight}{Tensor. Output projection weight matrix.}

\item{out_proj_bias}{Tensor or NULL. Output projection bias vector.}

\item{key}{Optional key tensor of shape \verb{(.., src_len, embed_dim)}.
Required when \code{cached_kv} is \code{NULL}.}

\item{value}{Optional value tensor of shape \verb{(.., src_len, embed_dim)}.
Required when \code{cached_kv} is \code{NULL}.}

\item{cached_kv}{Optional \code{KVCacheEntry} for pre-computed key/value projections.
When provided:
\itemize{
\item \code{key} and \code{value} parameters are ignored
\item Only query projection is computed
\item \code{cached_kv$key} shape: \verb{(.., num_heads, src_len, head_dim)}
\item \code{cached_kv$value} shape: \verb{(.., num_heads, src_len, head_dim)}
\item RoPE is applied only to queries (keys should already have RoPE applied)
}}

\item{training}{Logical. Whether the model is in training mode (affects dropout)
(default: \code{TRUE}).}

\item{key_padding_mask}{Optional mask of shape \verb{(.., src_len)} that identifies
padding elements in the key sequence:
\itemize{
\item Binary masks: \code{TRUE} values indicate positions to ignore
\item Float masks: values are directly added to attention scores
}}

\item{attn_mask}{Optional attention mask of shape \verb{(tgt_len, src_len)} or
\verb{(.., num_heads, tgt_len, src_len)}.}

\item{rope}{Optional \code{RotaryEmbedding} for rotary positional encoding.}

\item{ssmax_layer}{Optional \code{nn_module}. If provided, applies scalable softmax
(SSMax) scaling to queries before attention computation.}

\item{need_kv}{Logical. If \code{TRUE} and \code{cached_kv} is \code{NULL}, also returns
the computed K and V projections along with the attention output
(default: \code{FALSE}).}
}
\value{
If \code{need_kv} is \code{FALSE} or \code{cached_kv} is provided:
Attention output tensor of shape \verb{(.., tgt_len, embed_dim)}.
If \code{need_kv} is \code{TRUE} and \code{cached_kv} is \code{NULL}:
List of \verb{(attn_output, k, v)} where:
\itemize{
\item \code{attn_output}: shape \verb{(.., tgt_len, embed_dim)}
\item \code{k}: shape \verb{(.., num_heads, src_len, head_dim)}
\item \code{v}: shape \verb{(.., num_heads, src_len, head_dim)}
}
}
\description{
Multi-head attention with support for rotary position embeddings
}
\keyword{internal}
