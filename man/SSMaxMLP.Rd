% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssmax.R
\name{SSMaxMLP}
\alias{SSMaxMLP}
\title{SSMaxMLP -- Scalable Softmax with MLP-Computed Scaling}
\usage{
SSMaxMLP(num_heads, n_hidden = 64L, elementwise = FALSE, head_dim = NULL)
}
\arguments{
\item{num_heads}{\code{integer(1)} Number of attention heads.}

\item{n_hidden}{\code{integer(1)} Number of hidden units in the MLP.
Default: \code{64L}.}

\item{elementwise}{\code{logical(1)} If \code{TRUE}, apply elementwise
scaling per head dimension so that each element in the head
dimension gets its own scaling factor.  Default: \code{FALSE}.}

\item{head_dim}{\code{integer(1)} or \code{NULL}. Dimension of each
attention head. Required when \code{elementwise = TRUE}.}

\item{q}{\code{tensor} of shape \code{(bs, n_heads, seq_len, head_dim)}.
Query tensor after projection.}

\item{n}{\code{integer(1)} Source sequence length.}
}
\value{
An \code{\link[torch]{nn_module}} object (class generator).

Scaled query tensor, same shape as \code{q}.
}
\description{
Applies scaling to queries:
\eqn{q_{\text{scaled}} = q \cdot \text{mlp}(\log n)},
where a small MLP learns to map sequence length to scaling factors.

Apply SSMax-MLP scaling to queries.
}
\seealso{
\code{\link{create_ssmax_layer}}
}
